@model Proyecto_Majestic.Distrito

@{
    ViewBag.Title = "Details";
}

<h2>Ver Distrito</h2>
<hr />

<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.nom_dis)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.nom_dis)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.est_dis)
        </dt>

        @if (Model.est_dis == true)
        {
            <dd>Habilitado</dd>
        }
        else
        {
            <dd>Deshabilitado</dd>
        }

    </dl>
</div>
<p>
    <div class="mb-3"></div>
    <div>
        @Html.ActionLink("Actualizar", "Edit", new { id = Model.cod_dis }, new { @class = "btn btn-primary" })
    </div>
    <div class="mb-3"></div>
    <div>
        @Html.ActionLink("Volver", "Index", null, new { @class = "btn btn-secondary" })
    </div>
</p>
